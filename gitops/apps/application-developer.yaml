# Application de DEVELOPER.
# Sync automático (como dev/staging): commits no overlay developer são
# aplicados sozinhos no cluster.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dito-api-developer
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.io
spec:
  project: dito-internal-api
  source:
    repoURL: https://github.com/flaviolongue/teste-gcp-dito.git
    targetRevision: main
    path: manifests/overlays/developer
  destination:
    server: https://kubernetes.default.svc
    namespace: dito-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
