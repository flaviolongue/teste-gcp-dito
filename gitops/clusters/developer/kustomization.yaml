apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bootstrap do cluster developer: instala o ArgoCD + registra o root app-of-apps.
# É este path que o Terraform (developer-bootstrap) aplica.
resources:
  - ../../install
  - root.yaml
