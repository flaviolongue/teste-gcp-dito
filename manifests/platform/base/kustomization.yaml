apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Plataforma do cluster: namespace tools, o Gateway (Application LB) e a rota
# da UI do ArgoCD. Os overlays ajustam o IP e os hostnames por ambiente.
resources:
  - namespace.yaml
  - gateway.yaml
  - httproute-argocd.yaml
